import React from "react";
import './Toolbar.scss'
import Logo from "../../UI/Logo/Logo";

const Toolbar = () => (
    <header className="Toolbar">
        <div>MENU</div>
        <Logo />
        <nav>
            ...
        </nav>
    </header>
)

export default Toolbar